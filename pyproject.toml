[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "srun_login"
version = "0.1.0"
dependencies = [
  "requests",
  "pycryptodome"
]

[project.scripts]
srun_login = "srun_login.srun_login:main"
