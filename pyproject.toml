[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "srun_login"
version = "0.0.4"
dependencies = [
  "requests",
]

[project.scripts]
srun_login = "srun_login.__main__:login"
